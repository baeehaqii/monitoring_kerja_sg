name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check SSH connectivity
        run: |
          echo "Runner public IP: $(curl -s https://api.ipify.org)"
          nc -zv -w 10 ${{ secrets.PROD_SSH_HOST }} ${{ secrets.PROD_SSH_PORT }} 2>&1 \
            && echo "✅ Port ${{ secrets.PROD_SSH_PORT }} OPEN" \
            || echo "❌ Port ${{ secrets.PROD_SSH_PORT }} REFUSED/CLOSED – cek aaPanel Security"

      - name: Deploy to Server Kantor (task.siproper.com)
        uses: appleboy/ssh-action@v1.2.0
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          host: ${{ secrets.PROD_SSH_HOST }}
          username: ${{ secrets.PROD_SSH_USER }}
          key: ${{ secrets.PROD_SSH_PRIVATE_KEY }}
          port: ${{ secrets.PROD_SSH_PORT }}
          timeout: 60s
          command_timeout: 30m
          envs: GH_TOKEN
          script: |
            set -e

            PROJECT_DIR="/www/wwwroot/monitoring_kerja_sg"
            DEPLOY_USER="$(whoami)"

            echo "==> Fixing ownership to $DEPLOY_USER..."
            sudo chown -R ${DEPLOY_USER}:${DEPLOY_USER} $PROJECT_DIR

            cd $PROJECT_DIR

            echo "==> Pulling latest code..."
            git config --global --add safe.directory $PROJECT_DIR
            git fetch https://x-access-token:${GH_TOKEN}@github.com/baeehaqii/monitoring_kerja_sg.git main
            git reset --hard FETCH_HEAD

            echo "==> Installing dependencies..."
            npm install --frozen-lockfile --cache /tmp/npm-cache-deploy

            echo "==> Generating Prisma client..."
            npx prisma generate

            echo "==> Running database migrations..."
            npx prisma migrate deploy

            echo "==> Building Next.js app..."
            npm run build

            echo "==> Restarting app via PM2 (www user)..."

            # Fix ownership back to www so PM2 can read the files
            sudo chown -R www:www $PROJECT_DIR

            # Reload/restart via PM2 (zero-downtime if possible)
            if sudo -u www pm2 describe monitoring_kerja_sg > /dev/null 2>&1; then
              sudo -u www pm2 reload monitoring_kerja_sg --update-env 2>&1
              echo "✅ PM2 reload selesai"
            else
              # First deploy: register ke PM2
              sudo -u www bash -c "cd $PROJECT_DIR && pm2 start npm --name monitoring_kerja_sg -- start"
              sudo -u www pm2 save
              echo "✅ PM2 start selesai (pertama kali)"
            fi

            sleep 5
            ss -tlnp | grep -q ':3003' && echo "✅ App listening on port 3003" || echo "❌ App belum running, cek: sudo -u www pm2 logs monitoring_kerja_sg"

            echo "==> Deploy complete!"
